# Copie este arquivo para ".env.local" (mesmo nome, sem ".example") e
# preencha com as chaves reais. O ".env.local" nunca deve ser compartilhado
# nem subido pro GitHub (o .gitignore já está configurado pra ignorá-lo).

OMDB_API_KEY=5d35ff85
TMDB_API_KEY=3cc4fa0abe696f59de02fd922e2a24f0

# Dropbox: refresh token PERMANENTE (não expira). Gere as 3 linhas abaixo
# rodando, uma única vez:
#   node scripts/get-dropbox-refresh-token.js
DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
DROPBOX_REFRESH_TOKEN=
